FROM node:20-alpine
WORKDIR /usr/src/app

# install deps first (better caching)
COPY package*.json ./
RUN npm ci

# app source is bind-mounted in compose for hot reload
EXPOSE 5173
CMD ["npm","run","dev","--","--host","0.0.0.0","--port","5173"]
